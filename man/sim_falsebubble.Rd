% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sim.R
\name{sim_falsebubble}
\alias{sim_falsebubble}
\title{Simulation of a deterministic technology-adoption "false bubble" null}
\usage{
sim_falsebubble(
  n,
  t1 = floor(0.3 * n),
  t2 = floor(0.7 * n),
  kappa = floor((t2 - t1)/2),
  shape = c("triangular", "gaussian"),
  amplitude = 1,
  mu = 0.02,
  sigma_d = 0.05,
  r = 0.05,
  d0 = 0,
  seed = NULL
)
}
\arguments{
\item{n}{A positive integer specifying the length of the simulated output series.}

\item{t1}{Adoption (ramp-up start) date, in \code{1:n}.}

\item{t2}{Maturation (shock end) date, in \code{t1:n}.}

\item{kappa}{Peak lag (time from \code{t1} to the hump's peak), in \code{0:(t2 - t1)}.}

\item{shape}{\code{"triangular"} (default) or \code{"gaussian"}.}

\item{amplitude}{A positive scalar scaling the hump's peak height.}

\item{mu}{A scalar, the baseline dividend-growth drift.}

\item{sigma_d}{A positive scalar, the dividend-growth innovation standard deviation.}

\item{r}{A positive scalar, the discount rate.}

\item{d0}{Starting (log) dividend level.}

\item{seed}{An object specifying if and how the random number generator (rng)
should be initialized. Either NULL or an integer will be used in a call to
\code{set.seed} before simulation. If set, the value is saved as "seed" attribute
of the returned value. The default, NULL, will not change rng state, and
return .Random.seed as the "seed" attribute. Results are reproducible
across the parallel and non-parallel option when the same seed is used.}
}
\value{
A numeric vector of length \code{n} (the price), with
\code{"dividend"} and \code{"technology"} attributes.
}
\description{
Simulation of Chen, Chen, Huang, Li & Zhang (2026)'s false-bubble DGP: a
hump-shaped, \emph{deterministic} technology-adoption shock embedded in
dividend growth, engineered so a Campbell-Shiller present-value
fundamental alone -- with \strong{no bubble component at all} -- displays a
locally explosive-looking price path. Useful as a null (no-bubble) stress
test distinct from a plain random walk.
}
\details{
Dividends follow a random walk with drift plus the technology hump:
\eqn{d_t = d_{t-1}+\mu+\tau_t+\eta_t}{d[t] = d[t-1] + mu + tau[t] +
eta[t]}. The hump \eqn{\tau_t}{tau[t]} rises linearly from \code{t1} to
\code{t1 + kappa} then falls linearly to \code{t2} (\code{shape =
"triangular"}, the source's own worked example, eq. 4), or follows a
Gaussian bump centered at \code{t1 + kappa} (\code{shape = "gaussian"}).
Because \eqn{\tau_t}{tau[t]} is deterministic (known in advance), its
contribution to the price is an exact forward-looking discounted sum,
\eqn{T_t=\sum_{s>t}\beta^{s-t}\tau_s}{T[t] = sum_{s>t} beta^(s-t)*tau[s]}
with \eqn{\beta=1/(1+r)}{beta = 1/(1+r)}, added to the same fundamental
pricing formula \code{\link{sim_div}} uses. This is a simplified,
single-shock reproduction of the source's mechanism (deterministic hump
-> hump-shaped fundamental price, no bubble), not its full DOLS/
multiple-functional-form robustness machinery.
}
\examples{
sim_falsebubble(200, seed = 123) \%>\%
  autoplot()
}
\references{
Chen, H., Chen, L., Huang, D., Li, Y. & Zhang, Z. (2026).
"Technology Fundamentals and False Bubble Detection: Evidence from
Dot-Com and AI Episodes." arXiv:2604.25826.
}
\seealso{
\code{\link{sim_div}}, \code{\link{sim_evans}}
}
