% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sim.R
\name{sim_vol_sv}
\alias{sim_vol_sv}
\title{Simulate AR(1) lognormal stochastic-volatility innovations}
\usage{
sim_vol_sv(n, phi = 0.98, tau = 0.1, log_sigma0_sq = 0, seed = NULL)
}
\arguments{
\item{n}{Number of innovations to generate.}

\item{phi}{AR(1) log-variance persistence, in (0, 1).}

\item{tau}{Positive standard deviation of the log-variance innovations.}

\item{log_sigma0_sq}{Starting value of \code{log(sigma^2)}. Defaults to 0.}

\item{seed}{An object specifying if and how the random number generator (rng)
should be initialized. Either NULL or an integer will be used in a call to
\code{set.seed} before simulation. If set, the value is saved as "seed" attribute
of the returned value. The default, NULL, will not change rng state, and
return .Random.seed as the "seed" attribute. Results are reproducible
across the parallel and non-parallel option when the same seed is used.}
}
\value{
A numeric vector of length \code{n}.
}
\description{
Generates shocks \code{z_t = sigma_t * eps_t} with a persistent AR(1)
log-variance, for use as \code{sim_psy1(..., e = sim_vol_sv(...))}.
}
\details{
\deqn{\log\sigma_t^2 = \phi\log\sigma_{t-1}^2 + \eta_t,\quad
\eta_t \sim iid\, N(0, \tau^2)}{log(sigma[t]^2) = phi*log(sigma[t-1]^2) +
eta[t], eta[t] ~ iid N(0, tau^2)}

with \code{phi} close to (but below) 1 for the "double local-to-unity"
near-integrated-variance case studied in the source.
}
\examples{
sim_vol_sv(199, seed = 1)
}
\references{
Sarkar, A. & Wells, M.T. (2025). "Double Local-to-Unity."
arXiv:2512.06823.
}
\seealso{
\code{\link{sim_psy1}}, \code{\link{sim_vol_cir}}
}
