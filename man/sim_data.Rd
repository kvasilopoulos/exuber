% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/doc-data.R
\docType{data}
\name{sim_data}
\alias{sim_data}
\alias{sim_data_wdate}
\title{Simulated dataset}
\format{
A tibble with 100 rows and columns \code{psy1} (\code{\link[=sim_psy1]{sim_psy1()}}),
\code{psy2} (\code{\link[=sim_psy2]{sim_psy2()}}), \code{evans} (\code{\link[=sim_evans]{sim_evans()}}), \code{div} (\code{\link[=sim_div]{sim_div()}}),
\code{blan} (\code{\link[=sim_blan]{sim_blan()}}); \code{sim_data_wdate} adds a \code{date} column
(monthly from 2000-01-01).

An object of class \code{tbl_df} (inherits from \code{tbl}, \code{data.frame}) with 100 rows and 6 columns.
}
\usage{
sim_data

sim_data_wdate
}
\description{
An artificial dataset containing series simulated from data generating processes
widely used in the literature on speculative bubbles. \code{sim_data} holds
five series of 100 observations each; \code{sim_data_wdate} is the same
data with an additional monthly \code{date} column. Both were generated by
\code{data-raw/sim-data.R} with \code{set.seed(1122)}.
}
\examples{
# Explore the five bundled series before running any test
matplot(sim_data, type = "l", lty = 1, xlab = "t", ylab = "value",
  main = "sim_data: the five bundled example series")
legend("topleft", legend = colnames(sim_data), col = 1:5, lty = 1, bty = "n")

\donttest{
# The usual next step: run radf() and plot with the bundled critical values
autoplot(radf(sim_data))
}
}
\seealso{
\code{\link[=sim_psy1]{sim_psy1()}}, \code{\link[=sim_psy2]{sim_psy2()}}, \code{\link[=sim_evans]{sim_evans()}}, \code{\link[=sim_div]{sim_div()}},
\code{\link[=sim_blan]{sim_blan()}} for the generating processes.
}
\keyword{datasets}
